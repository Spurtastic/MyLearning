## Basic user registration (Django)

* User logs in if they have the necessary credentials
* User registers if they dont have existing credentials
* User has password, typed twice, for confirmation
* User sees page with login and a small bottom layer with the “Don’t have Account ? Click here”

